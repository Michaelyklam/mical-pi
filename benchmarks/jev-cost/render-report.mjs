import fs from 'node:fs';
import path from 'node:path';
const root=process.env.JEV_BENCH_OUT??'/tmp/jev-cost-pilot';
const r=JSON.parse(fs.readFileSync(path.join(root,'summary.json'),'utf8'));
const output=process.argv[2];if(!output)throw Error('Supply a temporary output directory');
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const usd=x=>x===null||!Number.isFinite(x)?'—':'$'+x.toFixed(x<0.01?6:4);
const table=(headers,rows)=>`<div class="scroll"><table><thead><tr>${headers.map(h=>'<th>'+esc(h)+'</th>').join('')}</tr></thead><tbody>${rows.map(row=>'<tr>'+row.map(c=>'<td>'+esc(c)+'</td>').join('')+'</tr>').join('')}</tbody></table></div>`;
const aggregation=groups=>table(['Strategy','Verified success','Total API-equivalent + Jev','Cost / verified success','Coding-model input / cache-read tokens'],Object.entries(groups).map(([k,v])=>[k,`${v.solved}/${v.runs}`,usd(v.totalUsd),usd(v.usdPerSuccess),`${v.input??0} / ${v.cacheRead??0}`]));
const context={};for(const v of Object.values(r.context)){const x=context[v.arm]??={runs:0,solved:0,total:0,filter:0,input:0,cache:0};x.runs++;if(v.status==='completed'&&v.grade?.passed)x.solved++;x.total+=v.accountedUsd??0;x.filter+=v.filter?.accountedUsd??0;x.input+=v.generation?.usage?.input??0;x.cache+=v.generation?.usage?.cacheRead??0;}
const luna=r.baseline.luna,astra=r.baseline.astra;
const ratio=luna.usdPerRun>0?astra.usdPerRun/luna.usdPerRun:0;
const conclusion=luna.solved===luna.runs&&astra.solved===astra.runs
 ? `Both fixed models passed every measured baseline run. Luna’s average API-equivalent cost was ${ratio.toFixed(1)}× lower. These small tasks did not establish a quality advantage for Astra, so a router must beat Luna alone—not just the more expensive Astra baseline.`
 : 'The fixed-model baseline contains failures. Compare verified success before treating a lower dollar total as a saving.';
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Jev coding-cost pilot results</title><style>body{font:17px/1.6 system-ui;background:#10151c;color:#e5ebf2;max-width:1080px;margin:auto;padding:30px 22px}h1,h2{line-height:1.2}h1{font-size:38px}h2{margin-top:42px;color:#94d8cf}a{color:#9cc7ff}td,th{text-align:left;vertical-align:top;padding:12px;border-bottom:1px solid #374452}table{border-collapse:collapse;width:100%;font-size:15px}.scroll{overflow:auto}.card{background:#1e2b38;padding:22px;border-radius:12px;margin:18px 0}.muted{color:#aeb9c6}.numbers{display:flex;gap:18px;flex-wrap:wrap}.numbers .card{flex:1;min-width:220px}.big{font-size:28px;font-weight:700}li{margin:9px 0}code{font-size:.9em}</style><header><p class="muted">Controlled synthetic pilot · ${esc(r.generatedAt.slice(0,10))}</p><h1>Jev, Astra and Luna: what the pilot actually measured</h1><p class="card">${esc(conclusion)}</p><p><strong>Recommendation: don’t change the default harness yet.</strong> Use these runs to choose a larger real-repository test. They do not establish production savings, long-session compaction quality, or subscription-bill reductions.</p></header>
<h2>The useful findings</h2><ul><li><strong>Tool selection:</strong> Jev initial selection (${usd(r.variants['jev-initial-tools'].totalUsd)}) essentially tied the fixed local tool set (${usd(r.variants['fixed-local-tools'].totalUsd)}). No convincing Jev-specific advantage in three tasks.</li><li><strong>Context:</strong> fixed lexical filtering (${usd(context.deterministic?.total)}) matched Jev filtering (${usd(context.jev?.total)}) on quality and cost slightly less. The cheapest control simply omitted redundant notes.</li><li><strong>Bulk screening:</strong> Jev’s classifier was cheaper, but its three false alarms made the repair pipeline cost ${usd(r.screen.jev?.totalUsd)} versus Luna’s ${usd(r.screen.luna?.totalUsd)}. Both ended with 16/16 correct items.</li><li><strong>Verification:</strong> Jev unnecessarily escalated ${r.cascade.jev?.escalations}/8 correct Luna patches at the preset threshold. The replay cost rose from ${usd(r.cascade.none?.totalUsd)} to ${usd(r.cascade.jev?.totalUsd)} without improving correctness.</li><li><strong>Observation masking:</strong> cost increased on these short traces. This does not contradict evidence for masking genuinely long histories.</li></ul><p>All figures below include classifier cost where applicable. <strong>No clear Jev advantage over the strongest simpler control emerged in this pilot.</strong></p>
<div class="numbers"><div class="card"><div class="big">${usd(r.accounting.actualUsd)}</div>Actual API-reported OpenRouter charges</div><div class="card"><div class="big">${usd(r.accounting.apiEquivalentUsd)}</div>Codex API-equivalent estimate, not a charge</div><div class="card"><div class="big">${usd(r.accounting.pendingUsd)}</div>Uncertain reservations still held</div></div>
<h2>1. Fixed-model coding baseline</h2><p>Eight JavaScript repair tasks, 58 hidden behavioral cases, two trials per model and task. Two tasks span multiple files. Both models use medium reasoning and the same six-turn limit. Verified success means all cases passed and the agent completed cleanly.</p>${aggregation(r.baseline)}
<p>Mean accumulated model-request time per baseline task: Luna ${(luna.meanModelRequestMs/1000).toFixed(1)}s; Astra ${(astra.meanModelRequestMs/1000).toFixed(1)}s. This excludes local tool/grader time and is not production end-to-end latency.</p>
<p>These are small, self-contained functions with explicit specifications. They are not representative of heavy repository work or planning. A perfect score here is a ceiling effect, not proof of general model equivalence. Repeated runs of the same tasks are not independent samples of real coding work.</p>
<h2>2. Model routing</h2><p>Selectors see the task and original buggy source, never the grading cases. Replay their selections against both measured baseline trials. Selector costs are included once per task per trial; the Luna batch-router cost is charged once per batch per trial.</p>${aggregation(r.routing)}
<p class="muted">Fixed rule: choose Astra for multi-file tasks or prompts mentioning fencing/cycles; otherwise Luna. Replay does not reproduce model-switching cache rebuilds, failure recovery, or production latency. No rule or Jev threshold was fitted to the outcomes.</p>
<h2>3. Tool selection and observation masking</h2><p>Paired subset: retry policy, lease state, dependency ordering. Sixteen short schemas; twelve external integrations are deliberately disconnected. The fixed-local control keeps read/write/example/discover without a model selector. Ordinary discovery begins with read/discover. Jev selection keeps discovery as an escape hatch; initial selection happens once, per-turn selection repeats its own cost.</p>${aggregation(r.variants)}
<p class="muted">The catalog is small and synthetic. This cannot validate savings from a large MCP catalog. “mask-old” removes older tool-result bodies, retaining the two newest; these short runs are not a long-history compaction test. One trial per variant means differences can reflect model sampling and cache variability. The fixed-local control was added after the initial variant runs as a sanity check; this is an exploratory pilot, not a preregistered trial.</p>
<h2>4. Context filtering</h2><p>Three coding tasks with eight retrieved-note blocks: seven unrelated fixture notes and one redundant target note. No artificial repeated padding. Original source and task instructions remain intact. Astra returns a file map in one completion. Include both the filter/summarizer and downstream generation.</p>${table(['Arm','Verified success','Total cost','Filter cost included','Astra input / cache read'],Object.entries(context).map(([k,v])=>[k,`${v.solved}/${v.runs}`,usd(v.total),usd(v.filter),`${v.input} / ${v.cache}`]))}
<p class="muted">Relevance is unusually easy: the target note duplicates information already present. The no-note control is important. This tests redundant retrieval noise, not preservation of essential facts from old history. It is not evidence that Jev should replace compaction.</p>
<h2>5. Cheap-first verification</h2><p>Jev inspects code using two bug-presence questions; escalate if either probability is at least 0.35. The ordinary-check control runs three independently written examples per task. Neither sees hidden grading cases. Escalation restarts with the measured Astra baseline; totals include Luna generation, verification and any Astra restart.</p>${table(['Policy','Verified success','Escalations','Total cost'],Object.entries(r.cascade).map(([k,v])=>[k,`${v.solved}/${v.count}`,v.escalations,usd(v.totalUsd)]))}
<p>Verifier sensitivity controls include the originally broken code and known-correct references. A miss is a broken implementation accepted as sound; a false alarm is correct code sent for unnecessary escalation.</p>${table(['Candidate set','Verifier','Bugs found','Bugs missed','False alarms','Correctly accepted'],Object.entries(r.verifiers).flatMap(([set,v])=>Object.entries(v).filter(([k])=>k!=='total').map(([method,x])=>[set,method,x.tp,x.fn,x.fp,x.tn])))}
<p class="muted">If Luna produces no faulty patches, this pilot cannot estimate verifier sensitivity on realistic Luna failures. Known-broken controls are useful, but their bug distribution is different.</p>
<h2>6. Bulk screening with downstream repair</h2><p>The same batch contains eight broken and eight correct implementations, with opaque IDs and mixed order. These are paired versions of eight tasks, which makes comparisons easier than an unrelated production batch. Report classifier errors and total workflow cost, not classifier price alone. Repair replays measured Astra work on the original broken code or already-correct source. Review-all is the no-screening control; ordinary checks are the deterministic control.</p>${table(['Screener','Bugs missed','False alarms','Invalid verdicts','Correct after pipeline','Classifier only','Total incl. repair'],Object.entries(r.screen).map(([k,v])=>[k,v.fn,v.fp,v.invalid,`${v.solved}/${v.total}`,usd(v.classifierUsd),usd(v.totalUsd)]))}
<p class="muted">Missing/invalid verdicts fail closed and incur review. Combined workflows are replay estimates, not fresh end-to-end pipeline trials. A cheaper pipeline that leaves bugs unfixed has not won.</p>
<h2>Spending, interruptions and scope</h2><p>${r.accounting.requests} settled benchmark requests. Total committed against the $50 ceiling: <strong>${usd(r.accounting.committedUsd)}</strong>, including uncertain reservations. The initial blocked Jev request retains $0.01. An interrupted Astra request retains its full possible cost; no zero-cost assumption is made.</p><p>A sandbox audit found that a Node VM alone was insufficient isolation. The run was paused, and generated-code execution moved to nonroot Docker containers with no network or host mounts, read-only filesystems and resource limits. The interrupted request remains reserved. Completed baseline behavior was rechecked in the container grader.</p><p>An initial screening batch accidentally exposed good/bad labels in item IDs. Those classifier results were discarded and rerun with opaque IDs; their cost remains experiment overhead. A regression test checks that truth labels cannot enter the screening payload.<br><br>Aggregate request totals include measured access checks, filters and interruptions with known usage. Parent research, coding of the harness, and subagent development used subscription resources outside these benchmark-request totals. They are not included in the API-equivalent figures.</p><p><strong>Actual subscription savings are not measured.</strong> Astra and Luna run through existing Codex OAuth. Their displayed dollar values are comparable API-price estimates, not incremental payments; Jev is an additional paid API.</p><h2>What to test next</h2><ol><li>Use larger real-repository tasks where Luna sometimes fails, with task-level holdouts for any routing thresholds.</li><li>Test ordinary deferred tool discovery before adding another model. Use a real large catalog and measure the extra discovery turns.</li><li>Test deterministic observation masking on genuinely long histories, including facts needed later. Do not infer compaction safety from redundant-note filtering.</li><li>Keep final behavioral checks. Narrow Jev judgments are not a replacement for tests or security review.</li></ol><p class="muted">Models: GPT 6 Astra and GPT 5.6 Luna on Codex; pinned Jev 1.13 on OpenRouter’s Decisions API. Full local artifacts contain requests, outputs, usage, graders and the budget ledger; this public report contains aggregates only.</p></html>`;
fs.mkdirSync(output,{recursive:true});fs.writeFileSync(path.join(output,'index.html'),html);
console.log(output);
