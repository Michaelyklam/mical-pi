---
name: linear-tickets
description: Access Linear tickets when given a Linear issue URL or identifier.
---

# Linear Tickets

The personal API key is stored in `~/.config/pi/linear.env` as `LINEAR_API_KEY`.

Use the Linear GraphQL endpoint: `https://api.linear.app/graphql`.
